<label for="{{inputName}}" class="input__label {{labelClasses}}">{{inputName}}</label>
<input
        id="{{inputName}}"
        type="{{type}}"
        class="input {{inputClasses}}"
        name="{{name}}"
        value="{{value}}"
        {{#if isDisabled}}
            disabled
        {{/if}}
>