<input
        id="{{id}}"
        type="{{type}}"
        class="input"
        name="{{name}}"
    {{#if value}}
        value="{{value}}"
    {{/if}}
    {{#if disabled}}
        disabled
    {{/if}}
    {{#if placeholder}}
        placeholder="{{placeholder}}"
    {{/if}}
>
